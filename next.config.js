/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  basePath: '/Suy-Ngam-Ve-Thanh-Giuse',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
